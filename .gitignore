# This repo is PUBLIC and publishes to https://guadalupetunon.github.io/
# Only the built site is committed: index.html + files/
#
# Everything below stays local (in Dropbox) and is never pushed —
# it contains working notes and internal comments.

# Build sources and tooling
content.toml
build.py
build_cv.py
template.html
update.sh

# Working notes — contains candid internal commentary. Do not commit.
NOTES.md

# Previous design, kept for reference
oldindex.html

# Full-size photo originals (1873x2078, ~800KB each). Only the web-sized
# crop files/Tunon_photo.jpg is published — the page renders it into a
# 180px box, so the originals were ~10x oversized and three of the four
# were never used at all. Keep them here locally; never publish them.
files/Tunon_photo_web*.jpg

# Design experiments and comparisons
preview-*.html
palette-preview.html
website-palette.html

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# One-off history-rewrite filter, kept for reference (see NOTES.md)
scrub-history.sh
